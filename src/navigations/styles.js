import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  dashBoardHeaderStyle: {
    drawerLabel: 'Select Content',
    headerStyle: {
      backgroundColor: '#000000',
    },
    headerTitleAlign: 'center',
    headerTintColor: '#FFF',
  },
});

export default styles;
